version: '3.8'

services:
  urbangear:
    build:
      context: .
      dockerfile: Dockerfile
    image: urbangear-prod:latest
    container_name: urbangear-viva-demo
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
